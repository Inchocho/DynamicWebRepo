Redirect
web Container는 Redirect 명령이 들어오면 웹 브라우저에게 다른 페이지로
이동하라고 명령을 내린다
그러면 웹 브라우저는 URL을 지시된 주소로 바꾸고 그 주소로 이동한다
이때 새로운 페이지에서는 request와 response 객체가 새롭게 생성된다

즉 처음 클라이언트가 입력했던 모든 내용들이 다음 페이지에서는 소실된다

(요청1(클라이언트) -> 응답1(redirect)(서버) -> 요청2(클라이언트) -> 응답2(서버))

고객(클라이언트)
URL(번호)
상담원(서버) 

고객이 상담원에게 123번으로 통화 (요청)
상담원 124번으로 다시 문의하세요 (응답 - redirect) // 즉 여기서 req,res는 종료 소멸함 

고객이 상담원에게 124번으로 통화 (요청2)